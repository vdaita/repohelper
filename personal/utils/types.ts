export default interface GithubRequestData {
    url: string;
}

export default interface StackOverflowRequestData {
    tag: string,
    count: number
}

export default interface URLRequestData {
    tag: string,
    count: number
}

